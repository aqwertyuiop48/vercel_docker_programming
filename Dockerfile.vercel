# Pull your pre-built Angular frontend image
FROM sreedockerhub19/my-angular-app-main:latest

# Explicitly tell Vercel we are using Port 80
ENV PORT=80
EXPOSE 80

# Run the standard Nginx entrypoint purely, exactly as it runs on your local machine
ENTRYPOINT ["/docker-entrypoint.sh", "nginx", "-g", "daemon off;"]